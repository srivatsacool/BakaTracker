/**
 * Test-only stub for the CJS-only `ajv-formats` package (peer of ajv).
 * Never called by the platform; only needs to load. See ajv.js.
 */
export default function addFormats(_ajv) {
  /* no-op */
}