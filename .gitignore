# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python environments & caches
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.uv/
.python-version
build/
develop-eggs/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# Environment variables (sensitive)
.env
.env.local
.env.*.local
*.env

# Local database/temporary files
*.db
*.sqlite

# IDE/Tool configs
.gemini/
.agents/
.copilot/

# Agent tooling & local caches (not product)
.hermes/
.claude/
.repowise/
.mcp.json
.vscode/mcp.json

# Private local reference implementation (never shipped)
cloudflare-reference/

# Google Cloud SDK
google-cloud-sdk/

# Editor local history — can contain secret .dev.vars snapshots
platform/.history/
.history/

# Playwright / browser E2E artifacts (screenshots, traces, videos, reports)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Extra / archive of unrelated, legacy, or throwaway files (not shipped)
extra/

# Impeccable live-mode local state (config, sessions, journal)
.impeccable/

# BakaTracker E2E — dedicated Firefox automation profile (local only, never committed)
.e2e/
